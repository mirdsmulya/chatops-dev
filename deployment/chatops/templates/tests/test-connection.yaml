apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chatops.fullname" . }}-test-connection"
  labels:
    {{- include "chatops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chatops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
